# AGENTS.md - Editor Adapter

Follow `PROJECT_BRAIN.md` for all project instructions.

Boot through `project-brain/MANIFEST.yaml` before handling any request. This file is
only an adapter for editors that automatically discover `AGENTS.md`; it must not
contain a separate instruction system.
