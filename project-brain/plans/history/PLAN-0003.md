---
id: PLAN-0003
status: completed
created_at: 2026-06-15T20:00:00+05:30
provider: OpenAI
model: GPT-5
editor: Codex
based_on_revision: unversioned
focus: "Implement a globally installable CLI that initializes project-brain in any project."
---

# Project Brain CLI Bootstrap

## Outcome

- Added the dependency-free `project-brain` npm executable.
- Added safe `init` and structural `validate` commands.
- Added neutral templates that mark generated project facts unverified.
- Renamed the universal entrypoint to `PROJECT_BRAIN.md`.
- Verified local tests, package contents, global-prefix installation, generated
  structure, and obsolete-name removal.

## Affected Domains

- npm package and executable command
- generated project-brain structure
- universal editor entrypoint and adapters
- manifest and freshness validation
- installation and usage documentation

## Implementation Order

1. Create the dependency-free npm package and command parser.
2. Build neutral installation templates separate from this repository's state.
3. Implement safe initialization, backups for forced replacement, and validation.
4. Rename the legacy entrypoint to `PROJECT_BRAIN.md` and update references.
5. Add automated and end-to-end tests.
6. Reconcile durable architecture, decisions, state, and changelog knowledge.

## Verification

- Install the package into a temporary npm prefix.
- Run `project-brain init` in clean and existing directories.
- Confirm default refusal and forced backup behavior.
- Run `project-brain validate`.
- Parse generated YAML and verify all boot paths.
- Confirm active files contain no obsolete product naming.

## Completion Criteria

- One global installation exposes the `project-brain` command.
- `project-brain init` safely creates a usable framework in any target project.
- Generated files contain neutral, unverified placeholders rather than invented facts.
- Automated tests pass and documentation matches actual command behavior.
